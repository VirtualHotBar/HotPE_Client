# HotPE客户端安全接口使用指南

## 概述
本指南介绍如何使用新的安全接口替代原有的不安全操作，确保代码的安全性和稳定性。

## 核心原则
1. **永远不要直接执行用户输入的命令**
2. **所有系统级操作都通过IPC调用主进程**
3. **使用提供的安全服务而不是直接调用工具**
4. **验证所有输入参数**

## 安全接口使用方法

### 1. 硬件信息获取

#### ❌ 旧的不安全方式
```typescript
// 危险：使用eval()
resolve(eval(`(${hwinfo})`));
```

#### ✅ 新的安全方式
```typescript
import { getHardwareInfo } from '../utils/hardwareInfo';

// 安全的硬件信息获取
const hwInfo = await getHardwareInfo('--cpu --memory');
console.log(hwInfo);
```

### 2. 磁盘操作

#### ❌ 旧的不安全方式
```typescript
// 危险：直接命令执行
await runCmdAsync(`${roConfig.path.tools}hdd.exe -mohong`);
```

#### ✅ 新的安全方式
```typescript
import { 
  getDisksInfo, 
  getPartitionsInfo, 
  getUsableLetter 
} from '../services/disk-service';

// 安全的磁盘信息获取
await getDisksInfo();
await getPartitionsInfo();
const letter = await getUsableLetter();
```

### 3. 安装操作

#### ❌ 旧的不安全方式
```typescript
// 危险：直接拼接用户输入
const cmd = `${pacmdPath} ${cmd} /out:${logPath}`;
await runCmdAsync(cmd);
```

#### ✅ 新的安全方式
```typescript
import { runSafePacmd, runSafeBootice } from '../services/install-service';

// 安全的PACMD执行
const result = await runSafePacmd('/hd:0 /del:all');
if (result.success) {
  console.log('操作成功');
}

// 安全的BOOTICE执行
const bootResult = await runSafeBootice(['/DEVICE=0', '/mbr', '/install']);
```

## 迁移步骤

### 步骤1：识别不安全代码
查找以下模式的代码：
- `eval()` 调用
- `runCmdAsync()` 直接拼接用户输入
- 直接执行系统工具

### 步骤2：导入安全服务
```typescript
// 硬件信息
import { getHardwareInfo } from '../utils/hardwareInfo';

// 磁盘操作
import { 
  getDisksInfo, 
  getPartitionsInfo, 
  runPacmd 
} from '../services/disk-service';

// 安装操作
import { 
  runSafePacmd, 
  runSafeBootice, 
  runSafePecmd 
} from '../services/install-service';
```

### 步骤3：替换不安全调用
```typescript
// 替换前
const disks = await runCmdAsync(`${toolPath}hdd.exe -mohong`);

// 替换后
await getDisksInfo(); // 结果自动更新到config中
const disks = config.environment.ware.disks;
```

### 步骤4：错误处理
```typescript
try {
  const result = await runSafePacmd('/hd:0 /init');
  if (!result.success) {
    console.error('操作失败:', result.output);
    // 处理失败情况
  }
} catch (error) {
  console.error('执行异常:', error);
  // 处理异常情况
}
```

## 常见迁移场景

### 场景1：磁盘分区操作
```typescript
// 旧代码
async function createPartition(diskIndex: string) {
  const cmd = `${pacmdPath} /hd:${diskIndex} /cre /size:1024`;
  await runCmdAsync(cmd);
}

// 新代码
async function createPartition(diskIndex: number) {
  const result = await runSafePacmd(`/hd:${diskIndex} /cre /size:1024`);
  return result.success;
}
```

### 场景2：引导安装
```typescript
// 旧代码
async function installBootloader(diskIndex: string) {
  await runCmdAsync(`${booticePath} /DEVICE=${diskIndex} /mbr /install`);
}

// 新代码
async function installBootloader(diskIndex: number) {
  const result = await runSafeBootice([
    `/DEVICE=${diskIndex}`, 
    '/mbr', 
    '/install'
  ]);
  return result.success;
}
```

### 场景3：文件属性设置
```typescript
// 旧代码
await runCmdAsync(`attrib ${filePath} +S +H`);

// 新代码
await setSafeFileAttributes(filePath, 'SH');
```

## 参数验证指南

### 磁盘索引验证
```typescript
function validateDiskIndex(index: any): number | null {
  const num = typeof index === 'string' ? parseInt(index, 10) : index;
  if (isNaN(num) || num < 0 || num > 99) {
    return null;
  }
  return num;
}
```

### 盘符验证
```typescript
function validateDriveLetter(letter: string): string | null {
  const match = letter.match(/^([A-Z]):?$/i);
  return match ? `${match[1].toUpperCase()}:` : null;
}
```

### 文件路径验证
```typescript
function isValidPath(filePath: string): boolean {
  // 检查路径遍历
  if (filePath.includes('..')) return false;
  
  // 检查危险字符
  if (/[<>:"|?*]/.test(filePath)) return false;
  
  return true;
}
```

## 调试和日志

### 启用详细日志
```typescript
// 在开发环境中启用详细日志
if (process.env.NODE_ENV === 'development') {
  console.log('执行安全命令:', command, args);
}
```

### 错误追踪
```typescript
try {
  const result = await runSafePacmd(command);
  if (!result.success) {
    console.error('命令执行失败:', {
      command,
      output: result.output,
      timestamp: new Date().toISOString()
    });
  }
} catch (error) {
  console.error('命令执行异常:', {
    command,
    error: error.message,
    stack: error.stack,
    timestamp: new Date().toISOString()
  });
}
```

## 性能优化建议

### 1. 批量操作
```typescript
// 避免频繁的IPC调用
const commands = [
  '/hd:0 /del:all',
  '/hd:0 /init',
  '/hd:0 /cre /size:1024'
];

// 考虑批量执行（如果后端支持）
for (const cmd of commands) {
  await runSafePacmd(cmd);
}
```

### 2. 缓存结果
```typescript
// 缓存磁盘信息，避免重复获取
let diskInfoCache = null;
let cacheTime = 0;

async function getCachedDiskInfo() {
  const now = Date.now();
  if (!diskInfoCache || now - cacheTime > 30000) { // 30秒缓存
    await getDisksInfo();
    diskInfoCache = config.environment.ware.disks;
    cacheTime = now;
  }
  return diskInfoCache;
}
```

## 测试建议

### 单元测试
```typescript
describe('安全磁盘操作', () => {
  test('应该安全获取磁盘信息', async () => {
    await getDisksInfo();
    expect(config.environment.ware.disks).toBeDefined();
    expect(Array.isArray(config.environment.ware.disks)).toBe(true);
  });

  test('应该验证磁盘索引', async () => {
    const result = await runSafePacmd('/hd:999 /init'); // 无效索引
    expect(result.success).toBe(false);
  });
});
```

### 集成测试
```typescript
describe('安装流程集成测试', () => {
  test('应该完成完整的U盘安装流程', async () => {
    // 模拟完整的安装流程
    await getDisksInfo();
    const result = await runSafePacmd('/hd:0 /del:all');
    expect(result.success).toBe(true);
    // ... 更多测试步骤
  });
});
```

## 常见问题解答

### Q: 为什么要迁移到IPC调用？
A: IPC调用将危险操作隔离在主进程中，可以进行更严格的安全控制，防止代码注入和命令注入攻击。

### Q: 性能会受到影响吗？
A: IPC调用的开销很小（<5ms），对用户体验几乎没有影响。

### Q: 如何处理现有代码的兼容性？
A: 我们提供了兼容性函数，可以逐步迁移现有代码，不需要一次性全部修改。

### Q: 如何确保新接口的安全性？
A: 所有新接口都经过严格的参数验证、路径检查和命令清理，并且在主进程中受控执行。

---

**更新时间**: 2025年9月11日  
**版本**: v1.0  
**适用范围**: HotPE客户端 v2.0+